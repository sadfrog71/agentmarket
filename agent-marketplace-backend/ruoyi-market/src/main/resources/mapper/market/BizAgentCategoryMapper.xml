<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.market.mapper.BizAgentCategoryMapper">

    <resultMap id="BizAgentCategoryResult" type="com.ruoyi.market.domain.BizAgentCategory">
        <id property="categoryId" column="category_id" />
        <result property="categoryCode" column="category_code" />
        <result property="categoryName" column="category_name" />
        <result property="description" column="description" />
        <result property="iconCode" column="icon_code" />
        <result property="sortNo" column="sort_no" />
        <result property="status" column="status" />
        <result property="delFlag" column="del_flag" />
        <result property="createBy" column="create_by" />
        <result property="createTime" column="create_time" />
        <result property="updateBy" column="update_by" />
        <result property="updateTime" column="update_time" />
        <result property="remark" column="remark" />
    </resultMap>

    <sql id="categoryColumns">
        category_id, category_code, category_name, description, icon_code,
        sort_no, status, del_flag, create_by, create_time, update_by, update_time, remark
    </sql>

    <select id="selectCategoryById" resultMap="BizAgentCategoryResult">
        select <include refid="categoryColumns"/> from biz_agent_category
        where category_id = #{categoryId} and del_flag = '0'
    </select>

    <select id="selectCategoryByCode" resultMap="BizAgentCategoryResult">
        select <include refid="categoryColumns"/> from biz_agent_category
        where category_code = #{categoryCode} and del_flag = '0'
        limit 1
    </select>

    <select id="selectCategoryList" parameterType="com.ruoyi.market.domain.BizAgentCategory" resultMap="BizAgentCategoryResult">
        select <include refid="categoryColumns"/> from biz_agent_category
        <where>
            del_flag = '0'
            <if test="categoryName != null and categoryName != ''">and category_name like concat('%', #{categoryName}, '%')</if>
            <if test="status != null and status != ''">and status = #{status}</if>
        </where>
        order by sort_no asc, category_id asc
    </select>

    <select id="selectEnabledCategoryList" resultMap="BizAgentCategoryResult">
        select <include refid="categoryColumns"/> from biz_agent_category
        where status = '0' and del_flag = '0'
        order by sort_no asc, category_id asc
    </select>

    <select id="countAgentsByCategoryCode" resultType="int">
        select count(1) from biz_agent
        where primary_category_code = #{categoryCode} and del_flag = '0'
    </select>

    <insert id="insertCategory" parameterType="com.ruoyi.market.domain.BizAgentCategory" useGeneratedKeys="true" keyProperty="categoryId">
        insert into biz_agent_category
        (category_code, category_name, description, icon_code, sort_no, status, del_flag, create_by, create_time, remark)
        values
        (#{categoryCode}, #{categoryName}, #{description}, #{iconCode}, #{sortNo}, #{status}, '0', #{createBy}, sysdate(), #{remark})
    </insert>

    <update id="updateCategory" parameterType="com.ruoyi.market.domain.BizAgentCategory">
        update biz_agent_category
        <set>
            category_name = #{categoryName}, description = #{description}, icon_code = #{iconCode},
            sort_no = #{sortNo}, status = #{status}, update_by = #{updateBy},
            update_time = sysdate(), remark = #{remark}
        </set>
        where category_id = #{categoryId} and del_flag = '0'
    </update>

    <update id="softDeleteCategoryByIds">
        update biz_agent_category set del_flag = '2', update_time = sysdate()
        where category_id in
        <foreach collection="array" item="categoryId" open="(" separator="," close=")">
            #{categoryId}
        </foreach>
    </update>
</mapper>
